const mergesort = (array) => {
  // Divide the array until individual elements are in their own arrays
  // Compare each value with it's neighbours while sorting them
  for(var i=0; i < array.length; i++) {
    let subsequentArray = array[i];

  }
}

console.log(mergesort([9, 2, -1, 22, 0, 300]));